import { ExpandDirective } from './expand.directive';

describe('ExpandDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandDirective();
    expect(directive).toBeTruthy();
  });
});
